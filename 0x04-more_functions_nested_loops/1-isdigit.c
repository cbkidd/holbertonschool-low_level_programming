#include "holberton.h"

/**
* _isdigit - returns 1 if c is a digit
* @c: ascii value tested
* Return: 0, or 1
*/

int _isdigit(int c)
{
	if (c > 47 && c < 58)
	{
	return (1);
	}
	else
	{
	return (0);
	}
return (0);
}
