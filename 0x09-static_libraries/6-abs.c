#include "holberton.h"

/**
* _abs - calculates absolute values
* @n: random number
* Return: absoulte value of n
*/

int _abs(int n)
{

	if (n > 0)
	{
	return (n * 1);
	}
	if (n < 0)
	{
	return (n * -1);
	}
	if (n == 0)
	{
	return (0);
	}
return (0);
}
